application-dependencies:
  default:
  - foo
  - something-else
  cpu:
  - bar
  - another-one
  third-platform:
  - example

components:
  - name: list
    command:
      executable: ls
      arguments:  -lth foo:ref
    references:
    - foo:ref