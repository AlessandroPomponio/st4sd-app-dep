application-dependencies:
  default:
  - foo
  - something-else
  cpu:
  - foo
  - bar
  - another-one
  third-platform:
  - foo
  - example

components:
  - name: list
    command:
      executable: ls
      arguments:  -lth foo:ref
    references:
    - foo:ref